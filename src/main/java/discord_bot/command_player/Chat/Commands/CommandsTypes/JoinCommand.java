package discord_bot.command_player.Chat.Commands.CommandsTypes;

public class JoinCommand extends Command {
    public JoinCommand() {
        super("!join",
                "Bot join the channel with user who sent message",
                null);
    }

}
