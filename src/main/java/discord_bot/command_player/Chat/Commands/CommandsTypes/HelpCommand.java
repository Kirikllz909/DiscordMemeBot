package discord_bot.command_player.Chat.Commands.CommandsTypes;

public class HelpCommand extends Command {
    public HelpCommand() {
        super("!help",
                "Get description for each command available for bot",
                null);
    }
}
